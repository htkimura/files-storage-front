import { DrivePageContent } from './DrivePageContent'

export const MyDrive = () => {
  return (
    <DrivePageContent
      folderId={null}
      title="My Drive"
      description="Drop files anywhere to upload. Drag items onto a folder or breadcrumb to move them."
    />
  )
}
